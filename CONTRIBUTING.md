# 협업 규칙 (Team Collaboration Guide)

> 14팀 · LLM 기반 GitHub 이슈·PR 자동 트리아지 에이전트 설계 및 성능 검증
>
> 개발 작업과 진행 상태는 GitHub Issues, 코드 검토는 Pull Request, 회의록·학습 자료·수행일지는 노션, 일상 소통은 카카오톡에서 관리합니다.

## 1. 팀 정보 및 역할

| 이름         | 주요 역할                                                                     |
| ---------- | ------------------------------------------------------------------------- |
| **여윤성·팀장** | 벤치마크 저장소 선정·GitHub Issue/PR 데이터 수집, 이슈 유형 분류·우선순위 추정, 전체 일정·Repository 관리 |
| **강세웅** | GitHub OAuth 인증·권한·Token/Secret 관리, LLM 후보 조사, 중복 이슈 탐지·PR 요약 로직 |
| **이형언**    | 평가지표 설계·측정, GitHub Actions 워크플로우 및 임베딩 캐싱 설계                              |

담당자는 해당 업무의 준비와 진행을 주도합니다. 프로젝트 범위, 인증 방식, 평가 기준처럼 전체 구현에 영향을 주는 사항은 팀이 함께 검토합니다.

## 2. 도구별 사용 범위

| 도구 | 관리할 내용 |
|---|---|
| GitHub Issues | 개발·조사·평가 작업, 버그, 담당자, 기한, 완료 기준, 진행 상황 및 작업별 결정 사항 |
| GitHub Pull Requests | 코드·저장소 문서 변경, 검토 의견, 검증 결과 및 병합 |
| 노션 | 차시별 회의록, 학습 자료, 활동 기록, 수행일지 및 결과보고서 초안 |
| 카카오톡 | 일정 조율, 급한 연락, 간단한 질문 |

- **작업 진행 상태의 기준은 GitHub Issues입니다.** 노션에 같은 작업의 상태를 별도로 관리하지 않고 관련 이슈·PR 링크를 기록합니다.
- 카카오톡에서 작업 범위·기한·구현 방식이 결정되면 담당자가 관련 이슈 또는 PR에 옮겨 적습니다.
- 회의의 전체 맥락은 노션에 기록하고, 실행할 과제는 GitHub 이슈로 등록하거나 기존 이슈를 갱신합니다.
- 노션의 팀 내부 문서를 공개 링크로 전환할 필요는 없습니다. PR 검토에 필요한 변경 목적·완료 기준·검증 결과는 GitHub에서도 확인할 수 있도록 작성합니다.

## 3. 정기 모임 및 회의록

- **일정:** 세부계획서의 차시별 일정에 맞춰 정기 모임을 진행합니다. 변경 시 관련 이슈의 기한도 함께 조정합니다.
- **시간:** 차시별 계획시간을 따르며, 회의·공동 실습·개발·검토를 포함합니다. 실제 활동시간은 별도로 기록합니다.
- **장소:** 정보공학관 프라임실·온라인을 활용하고 구체적인 방식은 모임 전에 공유합니다.
- **회의록 위치:** 노션의 팀 프로젝트 페이지에 `YYYY-MM-DD | N차시 | 회의 주제` 형식으로 작성합니다.
- **기록 담당:** 해당 차시를 주도하는 팀원이 맡습니다. 전체 참여 차시는 시작 시 기록 담당자를 정합니다.
- **기록 항목:** 참석자, 실제 일시·활동시간, 논의 내용, 결정 사항, 미해결 사항, 담당별 후속 과제 및 관련 이슈·PR 링크.

회의록은 모임 종료 후 가능한 한 당일 작성합니다. 후속 과제에는 담당자·기한·완료 기준을 명시합니다.

## 4. 작업 진행 절차

1. **작업 등록:** 시작 전에 GitHub 이슈를 만들고 목적·담당자·기한·완료 기준을 작성합니다.
2. **진행 표시:** 작업을 시작하면 진행 상태를 변경하고, 조사 결과·질문·막힌 점을 이슈 댓글로 공유합니다.
3. **변경 작업:** 코드나 저장소 문서를 수정하는 경우 작업 브랜치에서 진행합니다.
4. **검토 요청:** 변경을 PR로 올리고 관련 이슈, 변경 내용, 검증 결과를 기록합니다.
5. **완료 처리:** 승인·검증 후 병합하고, 이슈의 완료 기준이 모두 충족되면 결과를 남겨 닫습니다.

조사·학습·평가처럼 코드 변경이 없는 작업은 결과 요약과 자료 링크를 이슈에 남기고, 필요한 팀 검토를 거쳐 완료 처리합니다. 기존 작업의 작은 보완은 해당 이슈·PR에서 관리할 수 있으며, 독립적인 작업으로 커진 경우 새 이슈로 분리합니다.

## 5. GitHub 이슈 작성 규칙

- **제목:** `[영역] 작업 내용` 형식을 사용합니다. 예: `[api] 공개 저장소 이슈 조회 및 페이지네이션 구현`.
- **담당자:** 진행을 책임질 팀원 1명을 Assignee로 지정합니다. 공동 작업자는 본문에 적습니다.
- **기한:** 본문에 목표 완료일을 적고, 관련 차시가 있으면 함께 기재합니다. 마일스톤을 운영하는 경우 해당 마일스톤도 연결합니다.
- **완료 기준:** 확인 가능한 결과를 체크리스트로 작성합니다.
- **진행 공유:** 주요 결과, 기한 변경, 작업을 막는 문제를 댓글로 남깁니다.
- **완료·취소:** 완료 시 결과·검증 근거를 기록합니다. 취소하거나 다른 이슈와 통합할 때에는 이유와 관련 링크를 남깁니다.

이슈 본문 예시:

```markdown
## 목적
벤치마크 수집에 필요한 공개 저장소 이슈 데이터를 조회한다.

## 작업 내용
- 이슈 목록 조회 및 PR 구분
- 페이지네이션 처리
- 필요한 필드를 JSON으로 저장

## 완료 기준
- [ ] 2페이지 이상 조회하고 중복·누락 여부 확인
- [ ] 이슈와 PR을 구분하여 저장
- [ ] 제목·본문·번호·URL·원본 라벨 저장 확인
- [ ] 실행 방법 및 확인 결과 기록

## 일정
- 목표 완료일: YYYY-MM-DD
- 관련 차시: 2차시

## 관련 자료
- 참고 문서, 노션 자료 또는 관련 이슈 링크
```

원본 라벨은 평가 데이터로 보관하며, 유형 분류 모델의 입력에서는 제외합니다. 작업용·버그 제보용 기본 템플릿은 `.github/ISSUE_TEMPLATE/`에서 관리합니다.

## 6. 브랜치 및 PR 규칙

- `main`에는 검토된 변경을 병합하고, 직접 커밋·푸시하지 않습니다.
- 작업 브랜치는 `feature/{담당자}-{작업명}` 형식으로 만듭니다. 예: `feature/sewoong-oauth-flow`.
- PR은 검토 가능한 하나의 목적을 중심으로 작성합니다. 작업이 크면 나누고, 중간 의견이 필요하면 Draft PR을 활용합니다.
- PR은 **작성자 외 팀원 최소 1명의 승인** 후 병합합니다. 작성자는 검토 의견을 반영하고 필요한 확인을 완료합니다.
- 승인 이후 동작에 영향을 주는 변경을 추가하면 다시 검토를 요청합니다.
- 승인과 필요한 검증이 완료되면 작성자 또는 검토자가 병합할 수 있습니다.
- 병합은 `Squash and merge`를 기본으로 하며, 완료된 작업 브랜치는 삭제합니다.

PR 본문에는 다음을 포함합니다.

1. 관련 이슈: `#이슈번호`.
2. 변경 목적과 주요 변경사항.
3. 확인 방법·결과, 남아 있는 문제.
4. 필요한 참고 자료 또는 노션 문서 링크.

코드 변경에는 관련 기능의 테스트 또는 실행 확인 결과를 기록합니다. 문서만 변경한 경우에는 내용·링크·표기 검토 결과를 적습니다. 프롬프트·모델·평가 설정을 변경하면 사용 버전, 비교 데이터 및 변경 전후 결과를 남깁니다.

PR 병합만으로 이슈의 모든 완료 기준이 충족되는 경우 `Closes #이슈번호`를 사용합니다. 후속 실험·시연 등이 남아 있으면 관련 이슈만 연결하고 완료 후 직접 닫습니다.

팀장은 문서의 승인 규칙이 실제로 적용되도록 `main`에 PR 필수·최소 1명 승인 등의 브랜치 보호 규칙을 설정하고 적용 여부를 확인합니다. CI가 구성되면 필수 검증 항목을 팀이 정합니다.

## 7. 커밋 메시지

| 접두어 | 의미 |
|---|---|
| `feat:` | 새 기능 추가 |
| `fix:` | 버그 수정 |
| `docs:` | 문서 변경 |
| `refactor:` | 동작을 유지하는 코드 구조 개선 |
| `test:` | 테스트 추가·수정 |
| `chore:` | 빌드·설정 등 기타 작업 |

예: `feat: 이슈 유형 분류 프롬프트 추가`

메시지는 무엇을 변경했는지 구체적으로 작성합니다. Squash 병합 시 최종 커밋 메시지도 이 규칙에 맞춥니다.

## 8. 라벨 및 진행 상태

다음은 **팀 작업 관리용 라벨**입니다.

| 분류 | 라벨 | 의미 |
|---|---|---|
| 작업 유형 | `type:bug` | 프로젝트 코드·동작의 버그 수정 |
| 작업 유형 | `type:feature` | 기능 추가·개선 |
| 작업 유형 | `type:question` | 조사·논의·질문 |
| 작업 유형 | `type:docs` | 문서 작성·정리 |
| 우선순위 | `priority:high`, `priority:medium`, `priority:low` | 팀이 합의한 작업 우선순위 |
| 상태 | `status:todo`, `status:in-progress`, `status:review`, `status:done` | 예정·진행·검토·완료 |
| 영역 | `area:api` | GitHub API·인증·데이터 수집 |
| 영역 | `area:llm` | 프롬프트·에이전트 로직 |
| 영역 | `area:eval` | 평가지표·벤치마크 |
| 영역 | `area:actions` | GitHub Actions·캐싱 |

- 진행 상태 라벨은 동시에 하나만 사용하고, 담당자가 이전 상태를 제거한 뒤 새 상태로 변경합니다.
- 완료 시 `status:done`으로 변경하고 이슈를 닫습니다. 취소한 작업은 완료로 표시하지 않고, 이유를 기록한 뒤 진행 상태 라벨을 제거하여 닫습니다.
- 영역은 필요한 항목을 선택합니다. 우선순위는 기한과 다른 작업에 미치는 영향을 고려하여 정합니다.
- 처음부터 모든 라벨을 붙이는 데 시간을 쓰기보다 담당자·완료 기준·진행 상태를 명확히 관리합니다.

### 에이전트 분석 대상과의 구분

팀 작업 관리용 라벨은 에이전트가 예측할 분류 체계와 별도로 관리합니다. 예를 들어 `type:docs`를 협업에 사용하더라도 이를 모델의 예측 유형에 자동으로 추가하지 않습니다.

벤치마크 저장소의 기존 라벨은 공통 분류 체계로 변환하여 평가용 정답으로 활용합니다. 라벨 대응 기준은 기술 문서에 기록하고, 정답 라벨을 분류 모델의 입력에 포함하지 않습니다.

자동 트리아지 테스트는 별도의 팀 소유 공개 시연 저장소에서 진행합니다. 개발 저장소의 업무 이슈와 시연 저장소의 분석 대상 이슈를 구분하며, 초기 PR 시연은 시연 저장소 내부 브랜치에서 생성한 PR을 대상으로 합니다.

## 9. 문서 및 인증정보 관리

| 문서 | 관리 위치 |
|---|---|
| 협업 규칙 | 저장소 루트 `CONTRIBUTING.md` |
| 프로젝트 소개·실행 방법 | 저장소 루트 `README.md` |
| 설계·API·데이터·평가 관련 기술 문서 | 저장소 `docs/` |
| 회의록·학습 노트·수행일지·보고서 초안 | 노션 팀 프로젝트 페이지 |

- 코드 변경으로 실행 방법이나 설정이 바뀌면 관련 저장소 문서도 같은 PR에서 수정합니다.
- 노션에는 필요한 기술 문서의 GitHub 링크를 기록합니다. 같은 문서를 두 곳에 복사하여 각각 수정하지 않습니다.
- 토큰·API 키·실제 인증정보가 포함된 `.env` 파일은 커밋하지 않습니다. `.gitignore`에 해당 파일을 등록하고, 환경변수 이름과 안전한 예시는 `.env.example`에 작성합니다.
- 로컬 실행에는 환경변수, Actions 실행에는 필요한 Secrets를 사용합니다. 토큰 값이 실행 로그·캡처·이슈·PR·노션 문서에 포함되지 않도록 확인합니다.

## 10. 협업 규칙 변경

규칙 변경은 이슈에서 제안하고, 합의한 내용을 이 문서의 수정 PR에 반영합니다. 수정 PR이 승인·병합되면 적용하며, 변경 사유와 적용 시점을 노션 회의록에 기록합니다.
